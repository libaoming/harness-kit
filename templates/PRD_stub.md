# PRD — {{PROJECT}}

> [!TODO] 文档先行：本文件没写完不许写代码。PRD 是 features.json 的输入源。和用户一起填，别自己脑补。
> [!TODO] 开工前先吸收已有上下文：沿用项目领域词汇（domain-modeling），遵守已有架构决策 ADR（tech-architecture）。术语和决策与它们对齐，别另起一套。

## 背景与动机
### 问题陈述（用户视角）
> [!TODO] 用户当前遇到什么问题 / 痛点，谁在痛、痛在哪。先讲问题，别先讲方案。

### 方案概述（用户视角）
> [!TODO] 一句话讲清楚我们怎么解，用户能得到什么。细节留给 SPEC。

## 目标用户
> [!TODO]

## User Stories
> [!TODO] 编号列出，格式严格：**作为 [角色]，我想要 [能力]，以便 [收益]**。
> 力求详尽穷举。每条应能 1:1 落成一个 features.json 原子 feature，「以便」部分就是验收信号的来源。

1. 作为 ___，我想要 ___，以便 ___。
2. ...

## 技术约束
> [!TODO]

## 测试策略 / 验证接缝
> [!TODO] 不只是「可验证」，要定到「在哪一层验、用什么接缝」：
> - **优先用已有测试接缝**，没有再提新的，且选**最高可行层级**
> - 全库接缝数越少越好（理想 1 个）——接缝方案需与用户确认
> - 列出被测模块、参考的先例
> - 这一节直接喂给 features.json 的 `verify` 字段 + fixture 设计（fixture 先于代码）

## 验收标准
> [!TODO]（每条尽量可验证，对应上面的验证接缝）

## Out of Scope（范围外）
> [!TODO] 明确写出本期**不做**什么。这是 PRD 级全局边界，与每个 feature 的 `out_of_scope` 字段上下游呼应，从源头拦住范围蔓延、防 AI 联调复刻隐性功能。

## 里程碑
- {{MILESTONE}}：> [!TODO] MVP 范围

## 补充说明
> [!TODO] 其它需要交代的（可选）。
